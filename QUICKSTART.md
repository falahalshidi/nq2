# دليل البدء السريع ⚡

## 🚀 التثبيت والتشغيل

### 1. تثبيت المكتبات
```bash
npm install
```

### 2. تشغيل السيرفر المحلي
```bash
npm run dev
```

### 3. فتح المتصفح
افتح المتصفح واذهب إلى:
```
http://localhost:3000
```

## 📱 الصفحات المتاحة

| الصفحة | الرابط | الوصف |
|--------|--------|--------|
| لوحة التحكم | `/` | الصفحة الرئيسية مع الإحصائيات |
| الفروع | `/branches` | إدارة فروع المطعم |
| الموظفين | `/staff` | إدارة الموظفين |
| المنيو | `/menu` | إدارة قائمة الطعام |
| العروض | `/offers` | إدارة العروض والخصومات |
| الطابعات | `/printers` | إدارة الطابعات |
| نقاط البيع | `/pos` | إدارة نقاط البيع |
| التحليلات | `/analytics` | التحليلات المتقدمة |
| الإعدادات | `/settings` | إعدادات النظام |

## 🎨 الميزات الرئيسية

### Sidebar القابل للطي
- **فتح/إغلاق**: اضغط على أيقونة القائمة في الأعلى
- **اختصار لوحة المفاتيح**: `Ctrl/Cmd + B`
- **المحتوى لا يختفي**: عند فتح Sidebar، المحتوى يظل مرئياً

### الرسومات البيانية
- جميع الرسومات تفاعلية
- مرر الماوس على البيانات لرؤية التفاصيل
- متجاوبة مع جميع أحجام الشاشات

### التنقل
- استخدم القائمة الجانبية للانتقال بين الصفحات
- التنقل سريع وفوري بدون تحديث الصفحة

## ⚙️ الأوامر المتاحة

### التطوير
```bash
npm run dev          # تشغيل سيرفر التطوير مع Turbopack
npm run lint         # فحص الأخطاء
npm run type-check   # فحص أخطاء TypeScript
```

### الإنتاج
```bash
npm run build        # بناء المشروع للإنتاج
npm start           # تشغيل النسخة الإنتاجية
npm run analyze     # تحليل حجم الملفات
```

## 🎯 نصائح الاستخدام

### 1. الأداء الأمثل
- المشروع محسّن للسرعة
- لا حاجة لأي إعدادات إضافية
- التطبيق يعمل بسرعة قصوى افتراضياً

### 2. التخصيص
- الألوان في ملف `app/globals.css`
- البيانات في ملف `lib/constants.ts`
- المكونات في مجلد `components/`

### 3. إضافة صفحات جديدة
```typescript
// app/(dashboard)/new-page/page.tsx
export default function NewPage() {
  return (
    <div>
      <h1>صفحة جديدة</h1>
    </div>
  )
}
```

## 🔧 التخصيص

### تغيير الألوان
في ملف `app/globals.css`، يمكنك تعديل الألوان:
```css
:root {
  --primary: oklch(0.205 0 0);
  --secondary: oklch(0.97 0 0);
  /* ... */
}
```

### إضافة عنصر في Sidebar
في ملف `components/app-sidebar.tsx`:
```typescript
const menuItems = [
  // ... العناصر الموجودة
  {
    title: "عنصر جديد",
    icon: YourIcon,
    href: "/new-page",
  },
]
```

## 📊 البيانات التجريبية

حالياً، التطبيق يستخدم بيانات تجريبية (mock data) لعرض الميزات.
لربط البيانات الحقيقية:

1. أنشئ API endpoints في مجلد `app/api/`
2. استخدم `fetch` أو مكتبة مثل `axios` لجلب البيانات
3. استبدل البيانات التجريبية بالبيانات الحقيقية

مثال:
```typescript
// app/(dashboard)/branches/page.tsx
"use client"

import { useEffect, useState } from "react"

export default function BranchesPage() {
  const [branches, setBranches] = useState([])
  
  useEffect(() => {
    fetch('/api/branches')
      .then(res => res.json())
      .then(data => setBranches(data))
  }, [])
  
  // ... باقي الكود
}
```

## 🎨 التصميم

### الخطوط
- الخط الأساسي: Cairo (للعربية)
- تلقائياً يدعم العربية والإنجليزية

### الأيقونات
- المكتبة المستخدمة: Lucide React
- أيقونات حديثة وخفيفة
- سهلة الاستخدام والتخصيص

### المكونات
- shadcn/ui: مكتبة مكونات حديثة
- قابلة للتخصيص بالكامل
- متجاوبة ومتاحة (Accessible)

## 🚨 حل المشاكل الشائعة

### المشروع لا يعمل؟
```bash
# احذف node_modules وأعد التثبيت
rm -rf node_modules
npm install
npm run dev
```

### أخطاء TypeScript؟
```bash
# فحص الأخطاء
npm run type-check
```

### الأداء بطيء؟
- تأكد من استخدام `npm run dev` (مع Turbopack)
- تحقق من إغلاق البرامج الثقيلة الأخرى

## 📚 الموارد

- [Next.js Docs](https://nextjs.org/docs)
- [shadcn/ui Docs](https://ui.shadcn.com/)
- [Tailwind CSS Docs](https://tailwindcss.com/docs)
- [Recharts Docs](https://recharts.org/)

## 💡 أفكار للتطوير

### المرحلة التالية
- [ ] ربط قاعدة بيانات حقيقية
- [ ] إضافة نظام المصادقة (Authentication)
- [ ] إضافة نظام الصلاحيات (Authorization)
- [ ] تطبيق الإشعارات الفورية
- [ ] إضافة نظام الرسائل الداخلية
- [ ] تصدير التقارير (PDF/Excel)
- [ ] طباعة الفواتير
- [ ] دعم عدة لغات
- [ ] تطبيق Progressive Web App (PWA)
- [ ] دعم Offline Mode

## 🤝 الدعم

للأسئلة أو المساعدة، راجع:
- ملف `README.md` للتفاصيل الكاملة
- ملف `FEATURES.md` لقائمة الميزات
- الكود مُعلّق بشكل جيد للفهم السهل

---

**استمتع بالتطوير! 🚀**

